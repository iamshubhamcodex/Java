package javaTut;

//import m.MyStack;

//import java.util.List;
//import java.util.Stack;

public class _29Vector_Stack {

	public static void main(String[] args) throws Exception {
		
		//	STACK
		/**
		 *	In Stack the element, that is added at last is popped out(bahar nikalta) at first like stack of anything in container[jis cheez ko baad dalenge vahi pahle bahar aayega]
		 **/
		/**
		 *	Methods
		 *		push(E e)
		 *		pop()
		 *		peek()
		 *		search(E e)
		 *		empty() 
		 **/
//		Stack<Integer> st = new Stack<>();
//		st.push(12);
//		st.push(125);
//		st.push(12654);
//		st.push(64612);
////		int popped = st.pop();
//		
////		System.out.println(popped); 
//		System.out.println(st.peek()); 
//		System.out.println(st.isEmpty());
		 
//		MyStack<Integer> stack = new MyStack<>();
//		stack.push(452);
//		stack.push(455);
//		stack.push(485);
//		stack.push(445);
//		stack.push(485);
//		int popped = stack.pop();
////		int peeked = stack.peek();
////		System.out.println(peeked);
//		System.out.println(popped);
		 
		 
		 
		 
		//	VECTOR
			/**
			 *	The Vector class is an implementation of the List interface that allows us to create resizable-arrays similar to the ArrayList class.
			 *
			 * Java Vector(it's size increases by 2) vs ArrayList(it's size increases by 1.5)
			 * 
			 * In Java, both ArrayList and Vector implements the List interface and provides the same functionalities. However, there exist some differences between them. The Vector class synchronizes each individual operation. This means whenever we want to perform some operation on vectors, the Vector class automatically applies a lock to that operation. However, in ArrayList methods are not synchronized.
			 * 
			 * NOTE:- IT is recommended to use ArrayList in place of Vector because vectors are not thread safe and less efficient
			 **/
		
		
		
		
	}

}
